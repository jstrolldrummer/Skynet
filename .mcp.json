{
  "mcpServers": {
    "skynet-dropbox": {
      "command": "node",
      "args": ["connectors/dropbox/src/server.js"],
      "env": {
        "DROPBOX_ACCESS_TOKEN": "${DROPBOX_ACCESS_TOKEN}",
        "DROPBOX_REFRESH_TOKEN": "${DROPBOX_REFRESH_TOKEN}",
        "DROPBOX_APP_KEY": "${DROPBOX_APP_KEY}",
        "DROPBOX_APP_SECRET": "${DROPBOX_APP_SECRET}"
      }
    }
  }
}
