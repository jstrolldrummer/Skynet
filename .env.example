# Dropbox OAuth2 access token (required).
# Generate one from the Dropbox App Console: https://www.dropbox.com/developers/apps
DROPBOX_ACCESS_TOKEN=

# Port the HTTP server listens on (optional, defaults to 3000).
PORT=3000
